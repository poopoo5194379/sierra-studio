# Legacy server prototype

The former duplicate implementation has been removed. The canonical and
deployable cloud backend is `workers/api.ts`, configured by the root
`wrangler.toml`. Shared protocol changes belong in `src/domain`.
