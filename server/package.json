{
  "name": "sierrastudio-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute sierrastudio-db --file=./schema.sql"
  },
  "dependencies": {
    "zod": "^4.0.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250701.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.24.0"
  }
}
