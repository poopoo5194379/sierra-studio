# HTML 报告设计方法论与复用指南

> 基于 5 个标杆 HTML 样例沉淀  
> 适用范围：数据月报、社媒复盘、行业洞察、趋势白皮书、活动专题页、网页演示报告  
> 核心目标：以后做报告时，不从“画一个页面”开始，而从“选择交付形态—建立叙事—搭建稳定底座—配置视觉系统—验证交付”开始。
> 最近更新：2026-07-31；补充 SierraStudio 5 条项目任务线中的制作、编辑与交付经验。

---

## 1. 先说结论：好的 HTML 报告由五层组成

1. **交付形态层**：连续长页、16:9 分页演示、A4 打印报告，三者不能混做。
2. **内容叙事层**：先形成结论和章节节奏，再决定图表、卡片和图片。
3. **视觉系统层**：颜色、字体、间距、圆角、阴影、图片风格必须可配置，而不是散落在页面各处。
4. **组件与数据层**：结论卡、指标卡、趋势图、排名表、案例卡等应该模块化、数据驱动。
5. **交付与质量层**：浏览器展示、响应式、打印、PDF、离线打开、编辑能力分别验证。

最关键的方法是：

> **先确定读者如何消费报告，再确定画布；先确定每页要证明什么，再选择组件；先建立视觉规则，再开始堆内容。**

---

## 2. 五个样例提供了什么经验

### 2.1 样例定位对照

| 样例 | 主要形态 | 典型优势 | 最适合复用的部分 | 需要警惕的问题 |
|---|---|---|---|---|
| 博物馆奇妙日 | 品牌互动长页 | 强主题包装、章节动效、城市卡片、行动按钮 | 活动专题页的情绪营造、叙事式长页、轻互动 | 当前本地副本的 HTML 与资源目录层级不匹配，样式和脚本会失效；中文编码也有异常 |
| 香港保险品牌社媒监测月报 | 数据仪表盘式长页 | 粘性导航、多层数据表、趋势图、品牌排名、连续阅读 | 运营月报、品牌监测、多个章节的快速跳转 | 页面很长、横向内容有溢出风险；同时混用图表库会增加维护成本 |
| 美亚特医小红书账号复盘 | 简洁卡片式长页 | 首屏信息明确、环比指标直观、好/待优化案例成对呈现 | 周报、内容复盘、账号诊断、案例对照 | 页面结构较静态，数据与 DOM 耦合；后续批量更新时容易重复手工改写 |
| 世界杯熬夜需求洞察 | 16:9 网页演示报告 | 固定画布、章节分页、目录导航、图片与洞察强绑定 | 路演、提案、会议汇报、网页 PPT | 为个别页面叠加大量打印补丁，长期迭代容易出现 CSS 覆盖链 |
| 2025–2026 色彩趋势报告 | A4 可编辑报告 | 杂志化版式、38 个打印页、编辑侧栏、草稿、导出、打印检查 | 白皮书、趋势报告、研究报告、可交付 PDF | 单文件约 8.7 MB、可编辑节点约 949 个，能力强但复杂度和维护成本都很高 |

### 2.2 从样例中得到的共性规律

- 报告不是同一种网页：**阅读型、演示型、打印型**应有不同底座。
- 首屏的任务不是讲完内容，而是快速回答：
  - 这是什么报告？
  - 覆盖什么时间和对象？
  - 最重要的价值是什么？
- 一份好报告需要“页面节奏”：
  - 总结页负责定调；
  - 数据页负责证明；
  - 案例页负责解释；
  - 方法或机会页负责把观察变成行动。
- 图表不是装饰，必须服务于一句明确结论。
- 大量图片可以提升质感，但必须处理离线、体积、清晰度、裁切和版权。
- “可编辑”不能等于“所有东西都可编辑”；编辑粒度越细，维护成本越高。
- 打印不是浏览器页面的自然副产品，必须单独设计和测试。

---

## 3. 第一步：先选择正确的报告形态

### 3.1 连续滚动长页

适合：

- 月报、周报、账号复盘；
- 仪表盘；
- 在线阅读型研究摘要；
- 需要搜索、复制文字、快速跳转的报告。

基本特征：

- 页面宽度自适应；
- 内容按章节连续向下；
- 顶部或侧边有粘性导航；
- 图表和表格可展开或横向滚动；
- 重点结论在滚动过程中反复出现。

推荐画布：

```css
:root {
  --content-max: 1200px;
  --page-gutter: clamp(16px, 3vw, 40px);
}

.report {
  width: min(100% - 2 * var(--page-gutter), var(--content-max));
  margin-inline: auto;
}
```

不适合：

- 要求每一页视觉完全固定的路演；
- 对打印页码、分页位置有严格要求的正式白皮书。

### 3.2 固定 16:9 分页演示

适合：

- 客户提案；
- 会议汇报；
- 发布会；
- 洞察路演；
- 网页 PPT。

基本特征：

- 每页是固定比例画布；
- 一页只承载一个主要命题；
- 有页码、目录、上一页/下一页；
- 支持滚轮、方向键或按钮切换；
- 屏幕端优先保证“全页可见”。

推荐基准：

```css
:root {
  --slide-w: 1440px;
  --slide-h: 810px;
}

.slide {
  width: var(--slide-w);
  height: var(--slide-h);
  overflow: hidden;
}
```

浏览器中通过统一舞台缩放适配，不要让每页自行响应式变形。

不适合：

- 超长表格；
- 需要大量复制文字的研究正文；
- 不做重新排版就直接打印成 A4。

### 3.3 A4 打印报告

适合：

- 白皮书；
- 趋势报告；
- 咨询报告；
- 正式研究交付；
- PDF 下载件。

基本特征：

- 每页固定为 210 mm × 297 mm；
- 安全边距以毫米管理；
- 页眉、页脚、页码固定；
- 每一页独立检查溢出；
- 屏幕预览和打印模式分离。

推荐基准：

```css
:root {
  --a4-w: 210mm;
  --a4-h: 297mm;
  --safe-x: 12mm;
  --safe-y: 11mm;
}

@page {
  size: A4 portrait;
  margin: 0;
}

.print-page {
  width: var(--a4-w);
  height: var(--a4-h);
  padding: var(--safe-y) var(--safe-x);
  overflow: hidden;
  break-after: page;
}
```

不适合：

- 直接当移动端网页使用；
- 将 16:9 页面简单压缩后打印；
- 让内容自然流动并期待浏览器自动给出稳定分页。

### 3.4 形态决策表

| 问题 | 是 | 建议 |
|---|---|---|
| 用户主要在浏览器中连续阅读？ | 是 | 连续长页 |
| 需要会议全屏讲解，每页一个观点？ | 是 | 16:9 分页演示 |
| 需要正式 PDF、打印、固定页码？ | 是 | A4 打印报告 |
| 同时需要网页阅读与 PDF？ | 是 | 建立两套布局或共享数据、分别渲染 |
| 需要现场改字、移动卡片？ | 是 | 在对应底座上增加有限编辑能力 |

不要用一个 DOM 同时兼顾所有形态。更稳妥的做法是：

```text
共享内容模型
├─ Web 阅读渲染器
├─ 16:9 演示渲染器
└─ A4 打印渲染器
```

---

## 4. 第二步：把原始材料变成叙事，而不是直接排版

### 4.1 先写“一页一句话”

在制作 HTML 前，先为每一页或每一章节写一句结论：

```text
01 封面：这份报告研究什么
02 执行摘要：最重要的三个发现
03 数据说明：结论如何得到
04 趋势变化：发生了什么
05 原因解释：为什么发生
06 人群拆解：谁最典型
07 案例证明：真实内容如何体现
08 机会建议：品牌下一步做什么
09 行动计划：怎么执行和衡量
```

如果一页无法用一句话说清，它通常包含了两个命题，应拆页。

### 4.2 推荐的洞察链

```text
事实 → 差异 → 原因 → 人群/场景 → 机会 → 行动
```

例如：

```text
事实：夜间提神内容声量上涨
差异：健康型提神增速高于传统能量饮料
原因：消费者既要保持清醒，也担心熬夜负担
场景：凌晨观赛、第二天仍需上班
机会：低负担能量补给
行动：围绕“看球不透支”建立产品与内容组合
```

### 4.3 数据页必须同时有三样东西

1. **结论标题**：直接说变化或判断。
2. **证据主体**：图表、表格、案例或图片。
3. **解释与行动**：这意味着什么，下一步做什么。

不要使用只有名词的标题：

```text
弱：品牌声量
强：头部品牌声量集中，但第二梯队互动效率更高
```

### 4.4 控制单页信息量

建议的上限：

| 页面类型 | 建议上限 |
|---|---|
| 封面 | 1 个标题、1 个副标题、3 个以内标签 |
| 执行摘要 | 3–5 条核心结论 |
| 指标页 | 3–6 个关键指标 |
| 图表页 | 1 个主图，最多 2 个辅助图 |
| 对比页 | 2–4 个并列对象 |
| 案例页 | 2–6 个案例卡 |
| 表格页 | 8–12 行为宜，更多则拆页或滚动 |
| 行动建议页 | 3–6 个动作，明确负责人/阶段/指标 |

---

## 5. 第三步：建立可配置的视觉系统

### 5.1 设计令牌要先于组件

建议至少维护以下变量：

```css
:root {
  /* 品牌颜色 */
  --color-primary: #176b5b;
  --color-secondary: #2d7be8;
  --color-accent: #f28c38;
  --color-positive: #159a61;
  --color-warning: #d97706;
  --color-negative: #d94c4c;

  /* 中性色 */
  --color-bg: #f5f8f7;
  --color-surface: #ffffff;
  --color-text: #17202f;
  --color-muted: #6f7a86;
  --color-line: #dfe6e3;

  /* 字号 */
  --text-display: 56px;
  --text-h1: 36px;
  --text-h2: 26px;
  --text-h3: 18px;
  --text-body: 14px;
  --text-note: 12px;

  /* 空间 */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;

  /* 形状 */
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 24px;
  --shadow-card: 0 8px 24px rgb(20 40 32 / 8%);
}
```

### 5.2 颜色使用规则

- 主色负责品牌识别、章节标题和主要数据。
- 强调色只用于“需要被立即看到”的少量内容。
- 正负变化使用稳定语义色，不要每页重新定义。
- 大面积背景降低饱和度，数据重点提高明度或对比度。
- 同一页最好只有一个主色焦点。
- 图表颜色顺序必须固定，确保跨页同一对象保持同色。

### 5.3 字体层级

建议只保留 6 个层级：

| 层级 | 用途 |
|---|---|
| Display | 封面大标题、章节数字 |
| H1 | 页面主结论 |
| H2 | 模块标题 |
| H3 | 卡片标题、指标标题 |
| Body | 正文、表格 |
| Note | 来源、口径、脚注 |

中文报告常用字体栈：

```css
font-family:
  Inter,
  "Noto Sans SC",
  "PingFang SC",
  "Microsoft YaHei",
  sans-serif;
```

趋势、文化、审美类报告可以局部使用衬线字体做标题，但正文和数据仍建议使用无衬线字体。

### 5.4 网格与对齐

推荐 12 栏网格。常用组合：

- 12：单一大结论；
- 8 + 4：主图 + 指标/解读；
- 7 + 5：图表 + 案例；
- 6 + 6：两组对比；
- 4 + 4 + 4：三项洞察；
- 3 × 4：四个指标卡。

视觉质感往往来自一致的左边线，而不是更多装饰。

### 5.5 空间节奏

- 页面标题与内容区之间的距离应大于卡片内部距离。
- 模块之间的距离应明显大于模块内部距离。
- 卡片内边距保持统一。
- 长页每隔 2–3 个密集章节安排一页或一区“呼吸页”。
- 16:9 页面不要将内容顶满四边，保留稳定安全边距。

---

## 6. 第四步：建立报告组件库

### 6.1 基础家具

这些元素应统一管理，不进入内容编辑：

- 页眉或章节标签；
- 报告名称；
- Logo；
- 页脚；
- 数据来源；
- 页码；
- 导航按钮。

### 6.2 推荐组件

#### 结论卡

结构：

```html
<article class="insight-card">
  <span class="insight-card__index">01</span>
  <h3>结论标题</h3>
  <p>证据与解释。</p>
</article>
```

适合：执行摘要、趋势总结、机会点。

#### 指标卡

必须包含：

- 指标名称；
- 当前值；
- 环比或同比；
- 口径或时间；
- 可选的微型趋势。

不要只放一个巨大数字。

#### 图表卡

必须包含：

- 结论型标题；
- 图例；
- 图表；
- 数据口径；
- 一句解释。

#### 对比卡

适合样例中的“较好 × 待优化”：

```text
内容缩略图
标题
关键数据
为什么有效 / 为什么待优化
可复制动作
```

#### 案例卡

图片不是主体，洞察才是主体。建议卡片中至少包含：

- 案例名称；
- 截图或素材；
- 标签；
- 数据表现；
- 成功机制；
- 可复用建议。

#### 排名表

排名表中优先突出：

- 名次；
- 对象；
- 核心值；
- 变化；
- 异常点。

不要让十几个弱相关字段挤在同一张表中。

#### 机会矩阵

常用二维轴：

- 热度 × 增长；
- 用户价值 × 品牌适配度；
- 传播潜力 × 执行难度；
- 影响力 × 可控性。

矩阵必须解释象限含义，不能只放点。

#### 时间轴/行动路线图

适合把洞察转化为执行：

```text
阶段 → 核心目标 → 内容动作 → 渠道 → KPI → 风险
```

### 6.3 组件的三种状态

每个组件都应考虑：

1. **正常状态**：数据完整。
2. **空状态**：没有数据或图片时仍然可读。
3. **异常状态**：极长标题、极大数字、缺失图片、负值、零值。

---

## 7. 第五步：把数据与页面解耦

### 7.1 不要把所有数字直接写死在 HTML

推荐内容模型：

```js
const report = {
  meta: {
    title: "报告名称",
    period: "2026.07",
    source: "数据来源"
  },
  summary: [
    {
      title: "核心结论",
      evidence: "关键证据",
      implication: "业务含义"
    }
  ],
  metrics: [
    {
      id: "exposure",
      label: "曝光",
      value: 1243,
      change: 0.42,
      direction: "up"
    }
  ],
  charts: [],
  cases: []
};
```

渲染层只负责把模型变成页面。

### 7.2 数据口径也应结构化

```js
{
  value: 1243,
  unit: "次",
  period: "7.17–7.23",
  comparison: "环比",
  source: "平台公开数据",
  note: "剔除重复内容"
}
```

### 7.3 同一份数据，多种输出

理想结构：

```text
report-data.json
      │
      ├── web-report.html
      ├── slide-report.html
      └── a4-report.html
```

这样可以避免：

- 网页数据改了，PDF 没改；
- 图表数字和结论文字不一致；
- 同一案例在多个页面重复维护。

---

## 8. 图表方法：先选表达任务，再选图形

### 8.1 图形选择

| 分析任务 | 优先图形 |
|---|---|
| 时间趋势 | 折线图、面积图 |
| 多对象比较 | 横向条形图 |
| 构成占比 | 100% 堆叠条形图、环形图 |
| 排名变化 | 斜率图、哑铃图 |
| 两维关系 | 散点图、气泡图 |
| 转化过程 | 漏斗图 |
| 人群/场景链路 | 流程图、桑基图 |
| 机会优先级 | 四象限矩阵 |
| 多指标画像 | 小倍图，慎用雷达图 |

### 8.2 图表必须写结论

图表标题模板：

```text
[对象] + [变化/差异] + [业务含义]
```

例如：

```text
25–44 岁贡献主要讨论量，是熬夜观赛的核心消费人群
```

### 8.3 图表视觉原则

- 删除无意义网格线和边框。
- 轴标签少而清楚。
- 重点系列高亮，其他系列降饱和度。
- 颜色保持跨页一致。
- 重要节点直接标注，减少读者来回查图例。
- 柱形图从零开始；折线图若截断坐标轴必须说明。
- 同时展示绝对值与变化率时，明确主次，避免双轴误导。

### 8.4 图表技术建议

- 同一项目尽量只用一个图表库。
- 交互长页优先使用 Canvas 渲染，性能通常更稳。
- 需要浏览器内编辑或矢量导出时再考虑 SVG。
- 图表实例统一注册，在窗口缩放时批量 `resize()`。
- 打印前等待图表完成渲染。
- 对关键图表准备静态降级图，避免导出时空白。

样例中同时存在 ECharts、Chart.js 和手写 SVG。它们分别有效，但在同一报告里混用会带来：

- 颜色与字体配置重复；
- 响应式行为不一致；
- 打印时机不一致；
- 维护和体积上升。

---

## 9. 表格方法：报告表格不是原始数据表

### 9.1 只保留支持结论的字段

先问：

- 这张表要让用户比较什么？
- 哪一列决定排序？
- 哪一列需要强调？
- 哪些字段可以进入详情或附录？

### 9.2 推荐处理

- 表头固定；
- 首列或关键列固定；
- 数值右对齐；
- 单位写在表头；
- 条件色只突出异常或重点；
- 隔行色保持轻微；
- 长文本限制行数；
- 移动端允许横向滚动；
- 打印时拆成可控页数。

### 9.3 不要让横向滚动成为默认布局

若 1200 px 宽页面仍需要横向滚动，优先：

1. 删除弱字段；
2. 拆成两张表；
3. 将次要字段放入展开详情；
4. 缩短字段表达；
5. 最后才使用横向滚动。

---

## 10. 图片与素材方法：生成“可用于报告”的素材

### 10.1 素材不是越多越好

图片承担四种任务：

1. 定调：封面、章节幕；
2. 证明：社媒截图、品牌案例、产品图；
3. 解释：场景插画、流程图、概念图；
4. 分隔：缓解连续数据页的密度。

每张图必须有明确角色。

### 10.2 建议的素材清单

在生成或搜集素材前，先建表：

| 编号 | 页面 | 角色 | 内容描述 | 比例 | 安全区 | 风格 | 来源/版权 |
|---|---|---|---|---|---|---|---|
| IMG-001 | 封面 | 定调 | 清晨观赛场景 | 16:9 | 左侧留标题区 | 纪实、低饱和 | 待确认 |
| IMG-002 | 趋势 1 | 证明 | 社媒内容截图 | 4:5 | 无 | 原始截图 | 已授权 |
| IMG-003 | 机会页 | 解释 | 低负担能量补给组合 | 1:1 | 中央留主体 | 商业静物 | AI 生成 |

### 10.3 AI 生图提示词结构

```text
用途 + 主体 + 场景 + 构图 + 光线 + 色彩 + 材质/镜头 + 留白 + 禁止项
```

示例：

```text
用于 A4 趋势报告章节页的商业摄影。
一位年轻都市消费者在清晨客厅观看足球比赛，
桌面有低糖饮品、轻食和补水产品。
主体位于画面右侧，左侧保留 40% 干净留白用于中文标题；
柔和晨光，薄荷绿与暖橙点缀，低饱和、高级、自然真实，
35mm 纪实摄影质感。
不要出现文字、Logo、水印、畸形手指、过度霓虹、杂乱背景。
横向 16:9。
```

### 10.4 报告素材质量标准

- 尺寸适配最终容器，不靠 CSS 强行拉伸。
- 统一色温、饱和度和颗粒感。
- 人物、产品和文字有安全裁切区。
- 截图隐藏个人隐私和无关 UI。
- 所有素材记录来源和版权状态。
- 不把重要文字生成在图片里。
- AI 生成图必须人工检查手、文字、Logo、产品结构和文化细节。

### 10.5 图片技术策略

| 场景 | 建议 |
|---|---|
| 在线网页 | WebP/AVIF，按需加载 |
| 单文件离线交付 | 合理压缩后 Base64 内嵌 |
| A4 PDF | 以最终打印尺寸准备 150–220 PPI |
| 全屏 16:9 | 1920×1080 或更高 |
| 手机截图 | 保留足够像素，统一圆角和边框 |

Base64 适合“单文件交付”，但会显著增大 HTML。大报告应避免重复嵌入同一 Logo 或水印，可通过 CSS、SVG 符号或构建脚本复用。

---

## 11. 互动与编辑能力：分级实现

### 11.1 Level 0：纯阅读

- 无编辑；
- 只提供目录、滚动、分页；
- 最稳定，适合正式交付。

### 11.2 Level 1：内容编辑

- 点击“进入编辑”后开启；
- 仅关键文本 `contenteditable`；
- 页面家具不可编辑；
- 支持保存草稿与恢复。

### 11.3 Level 2：布局微调

- 卡片宽高；
- 表格列宽；
- 字号、行距；
- 少量位置微调；
- 必须有“归零”和“溢出检查”。

### 11.4 Level 3：结构编辑

- 复制/删除卡片；
- 调整顺序；
- 拖拽；
- 新增页面；
- 导出独立 HTML。

这一级已经接近小型排版工具，应单独设计数据模型和撤销机制。

### 11.5 编辑器原则

- 默认关闭编辑；
- 只开放有业务价值的对象；
- 选中状态必须明显；
- 所有更改可撤销或恢复；
- 编辑 UI 不进入打印；
- 保存时只存数据和样式差异，不存整个混乱 DOM；
- 对极端值和溢出持续提示。

色彩趋势样例说明了浏览器内编辑的上限很高，但也说明：可编辑节点越多，越需要清晰的对象模型，否则后期难以维护。

---

## 12. 风格元素与动效资产库

这一模块用于持续积累可复用的“视觉语言”，避免每次做新报告都从颜色、装饰和动画重新开始。

它不是一套固定模板，而是由四部分组成：

```text
风格 DNA
├─ 视觉令牌：颜色、字体、形状、材质
├─ 风格元素：背景、容器、标记、框架、装饰
├─ 动效语言：速度、方向、节奏、触发方式
└─ 组合预设：针对不同报告类型的成熟搭配
```

积累原则：

- 元素与内容分离：装饰元素不能写死业务文案。
- 风格与结构分离：同一个页面结构可以替换不同风格皮肤。
- 动效与可见性分离：关闭动画后，内容仍然完整可见。
- 单个元素可独立调用，组合预设可以一键套用。
- 每项资产记录适用场景、禁用场景和性能成本。

### 12.1 风格 DNA 分类

先为报告选择一个主风格，最多再叠加一个辅助风格。不要将所有好看的元素混在一起。

#### A. 编辑杂志风

关键词：

- 大标题；
- 强留白；
- 非对称网格；
- 衬线与无衬线混排；
- 图片裁切；
- 细线和页码；
- 克制的渐变。

适合：

- 趋势报告；
- 时尚、美妆、生活方式；
- 品牌白皮书；
- 高层洞察。

常用元素：

- 超大章节数字；
- 竖排英文；
- 图片跨栏；
- 引言大字；
- 细线索引；
- 半透明色块。

动效语言：

- 慢速淡入；
- 图片遮罩揭示；
- 标题按行出现；
- 页面切换平稳克制。

#### B. 瑞士信息设计风

关键词：

- 严格网格；
- 无衬线字体；
- 高对比；
- 小面积高纯度强调色；
- 编号与模块化信息。

适合：

- 数据报告；
- 咨询提案；
- 战略报告；
- 科技与商业主题。

常用元素：

- 网格点阵；
- 粗黑标题；
- 单色大数字；
- 规则线；
- 编号标签；
- 直角或小圆角容器。

动效语言：

- 快速而准确；
- 沿网格方向移动；
- 数字计数；
- 条形图依次生长；
- 少用弹跳。

#### C. 数据科技风

关键词：

- 深色背景；
- 发光线条；
- 网格坐标；
- 数据扫描；
- 高亮节点；
- 冷色渐变。

适合：

- AI、技术、产业；
- 数据平台；
- 发布会；
- 未来趋势。

常用元素：

- 扫描线；
- 发光圆点；
- 坐标网格；
- 数据流线；
- 玻璃面板；
- 状态指示灯。

动效语言：

- 扫描；
- 连线绘制；
- 节点脉冲；
- 数字滚动；
- 背景低速漂移。

注意：科技感最容易过度。正文和表格仍需保持高可读性。

#### D. 自然健康风

关键词：

- 低饱和绿色；
- 有机曲线；
- 柔和渐变；
- 轻阴影；
- 呼吸感；
- 温暖真实图片。

适合：

- 健康；
- 食品；
- 医疗科普；
- 可持续发展；
- 消费洞察。

常用元素：

- 有机色块；
- 柔光光晕；
- 叶片或水滴轮廓；
- 圆角卡片；
- 浅色纹理；
- 手写式强调线。

动效语言：

- 缓慢呼吸；
- 柔和浮现；
- 小幅上移；
- 曲线路径；
- 避免机械感过强的快速切换。

#### E. 文化档案风

关键词：

- 编号；
- 档案标签；
- 扫描纹理；
- 印章；
- 文物或历史图像；
- 纸张质感；
- 中英文索引。

适合：

- 博物馆；
- 城市文化；
- 历史研究；
- 艺术展览；
- 活动专题。

常用元素：

- 档案编号；
- 坐标和年代；
- 细密网格；
- 纸张颗粒；
- 扫描框；
- 印刷套色；
- 展签式说明。

动效语言：

- 扫描线；
- 档案抽取；
- 标签盖章；
- 图像逐步显影；
- 时间轴展开。

#### F. 社交内容运营风

关键词：

- 明快；
- 卡片化；
- 标签丰富；
- 截图驱动；
- 强对比指标；
- 正负案例并列。

适合：

- 社媒周报；
- 账号复盘；
- 内容诊断；
- Campaign 复盘；
- 电商运营。

常用元素：

- 平台色标签；
- 手机截图框；
- 上升/下降箭头；
- 好/待优化标记；
- 数据贴纸；
- 评论引用气泡。

动效语言：

- 卡片依次出现；
- 标签轻微弹入；
- 数据变化高亮；
- 对比区域左右进入；
- 动画时间短。

### 12.2 风格元素分层

建议把风格元素分成六层，每层单独维护。

| 层级 | 作用 | 典型资产 |
|---|---|---|
| 背景层 | 建立整体气氛 | 纯色、渐变、网格、噪点、光晕、纸张纹理 |
| 空间层 | 组织版面 | 页面边界、安全区、分栏、章节带、侧栏 |
| 容器层 | 承载信息 | 卡片、玻璃面板、标签、信息条、引用框 |
| 强调层 | 建立视觉焦点 | 大数字、色块、下划线、圆点、箭头、标记笔 |
| 媒体层 | 统一图片与图表 | 图片框、设备框、拼贴、遮罩、图例、数据标签 |
| 导航层 | 说明位置和进度 | 页码、章节索引、进度线、目录、翻页按钮 |

使用顺序应从底层到高层。不要先加装饰，再寻找内容位置。

### 12.3 可积累的背景元素

#### 纯色与轻渐变

最稳定，适用于大多数数据报告。

```css
.bg-soft-gradient {
  background:
    radial-gradient(circle at 15% 12%, rgb(124 216 143 / 18%), transparent 32%),
    linear-gradient(135deg, #f8fbf7, #eef7f2);
}
```

#### 网格点阵

用于科技、咨询和瑞士风。密度要低，不能干扰正文。

```css
.bg-dot-grid {
  background-image: radial-gradient(
    circle,
    rgb(25 45 38 / 12%) 1px,
    transparent 1px
  );
  background-size: 20px 20px;
}
```

#### 纸张颗粒

用于文化、趋势和杂志风。优先使用小尺寸无缝纹理或 CSS 噪点，不要重复嵌入大图。

#### 柔光光晕

用于健康、消费和审美主题。保持在背景边缘，不要放在正文后方。

#### 数据坐标

由细线、刻度、坐标数字组成，适合章节页或数据图周围。它应像“信息家具”，不能伪装成真实坐标。

### 12.4 可积累的容器元素

建议沉淀以下容器变体：

| 名称 | 视觉特征 | 适用内容 |
|---|---|---|
| `card-clean` | 白底、细边、轻阴影 | 通用指标和正文 |
| `card-tinted` | 主色浅底 | 核心结论 |
| `card-outline` | 无底色、强调边框 | 对比和补充 |
| `card-glass` | 半透明、模糊背景 | 少量科技或发布会页面 |
| `card-editorial` | 无边框、大留白、细分隔线 | 杂志与趋势 |
| `card-terminal` | 深色、等宽标签、状态灯 | 数据科技 |
| `card-archive` | 编号、展签线、纸张色 | 文化档案 |
| `card-social` | 截图框、标签、数据贴纸 | 内容运营 |

每个容器至少定义：

```text
背景
边框
圆角
内边距
阴影
标题样式
正文样式
高亮状态
打印状态
深色状态
```

### 12.5 可积累的强调元素

#### 章节数字

- `01 / 02 / 03`；
- 可做超大背景数字；
- 可做左上角索引；
- 不要与真实排名数字混淆。

#### Kicker 标签

```html
<span class="kicker">PART 01 · MARKET SIGNAL</span>
```

用于告诉读者当前章节和语境，不承担主标题任务。

#### 结论色条

适合月报和数据报告：

```css
.insight-rule {
  border-left: 4px solid var(--color-primary);
  padding-left: 14px;
}
```

#### 数据贴纸

用于社媒、零售和内容运营：

```text
↑ 42%
TOP 1
高互动
待优化
新增机会
```

贴纸数量每页不宜超过 3–5 个。

#### 引用标记

适合消费者原声、评论和访谈：

- 大引号；
- 用户身份标签；
- 来源；
- 高亮关键词。

#### 路径与连接线

用于流程、链路、因果关系。线条需要有明确方向，避免成为无意义装饰。

### 12.6 图片与媒体框架资产

建议积累：

- 16:9 全幅图框；
- 4:5 社媒图框；
- 1:1 产品图框；
- 手机截图框；
- 双图对比框；
- 三图拼贴；
- 图像遮罩；
- 图片题注；
- 品牌 Logo 墙；
- 案例证据框。

统一参数：

```css
:root {
  --media-radius: 16px;
  --media-border: 1px solid rgb(20 40 32 / 10%);
  --media-shadow: 0 12px 30px rgb(20 40 32 / 10%);
  --media-caption-size: 12px;
}
```

图片框架必须同时处理：

- 图片缺失；
- 图片比例不一致；
- 图片过暗或过亮；
- 长题注；
- 版权标记；
- 打印裁切。

### 12.7 动效设计原则

动效的优先级：

```text
帮助理解 > 建立节奏 > 表达品牌气质 > 装饰
```

适合的动效：

- 首屏轻微入场；
- 章节进入时渐显；
- 指标增长；
- 图表绘制；
- 导航状态变化；
- 案例卡展开；
- 当前页与下一页的明确过渡。

不建议：

- 所有元素同时漂浮；
- 长时间循环动画；
- 影响阅读的背景运动；
- 打印时仍保留动画初始态；
- 关键内容依赖动画结束后才出现；
- 同一页同时使用多种弹性曲线；
- 为普通表格逐行做长时间动画。

### 12.8 动效令牌

像管理颜色一样管理动画参数：

```css
:root {
  /* 时长 */
  --motion-instant: 100ms;
  --motion-fast: 180ms;
  --motion-base: 320ms;
  --motion-slow: 560ms;
  --motion-scene: 900ms;

  /* 缓动 */
  --ease-standard: cubic-bezier(.2, .8, .2, 1);
  --ease-enter: cubic-bezier(.16, 1, .3, 1);
  --ease-exit: cubic-bezier(.7, 0, .84, 0);
  --ease-emphasis: cubic-bezier(.34, 1.56, .64, 1);

  /* 位移 */
  --motion-distance-sm: 8px;
  --motion-distance-md: 20px;
  --motion-distance-lg: 48px;

  /* 错峰 */
  --stagger-tight: 40ms;
  --stagger-base: 80ms;
  --stagger-loose: 140ms;
}
```

推荐规则：

- Hover/按钮反馈：100–180 ms；
- 卡片进入：240–400 ms；
- 章节标题：400–600 ms；
- 页面切换：500–900 ms；
- 背景气氛：8–20 s 的低速循环；
- 单页主要动画总时长控制在 1.5 s 左右；
- 同一组元素错峰不超过 6–8 个。

### 12.9 动效基础动作库

#### Fade：淡入

适合正文、来源和辅助内容。

#### Rise：上移进入

适合标题、卡片和指标。位移控制在 8–24 px，避免“从页面底部飞入”。

#### Wipe：遮罩揭示

适合图片、章节标题和色块。杂志感较强。

#### Draw：线条绘制

适合趋势线、路径、关系图和章节规则线。

#### Count：数字增长

适合 KPI。最终数字必须先存在于 DOM 或无动画模式中。

#### Stagger：错峰出现

适合 3–6 个同类卡片。顺序应与阅读顺序一致。

#### Focus：焦点强调

通过颜色、透明度或轻微缩放突出一个数据对象，其他对象同步降权。

#### Expand：展开

适合案例详情、脚注和方法说明。必须提供明确的收起状态。

#### Progress：进度反馈

适合长页章节、分页报告和步骤流程。

#### Ambient：环境动效

低速光晕、颗粒、扫描或轻微漂移。必须低对比，并允许关闭。

### 12.10 推荐的进入动画实现

HTML：

```html
<article class="reveal" data-motion="rise">
  <h3>核心结论</h3>
  <p>结论说明。</p>
</article>
```

CSS：

```css
.reveal {
  opacity: 1;
  transform: none;
}

.motion-ready .reveal {
  opacity: 0;
  transform: translateY(var(--motion-distance-md));
  transition:
    opacity var(--motion-base) var(--ease-enter),
    transform var(--motion-base) var(--ease-enter);
}

.motion-ready .reveal.is-visible {
  opacity: 1;
  transform: none;
}
```

JavaScript：

```js
const reduceMotion = matchMedia("(prefers-reduced-motion: reduce)").matches;

if (!reduceMotion) {
  document.documentElement.classList.add("motion-ready");

  const observer = new IntersectionObserver(
    entries => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;
        entry.target.classList.add("is-visible");
        observer.unobserve(entry.target);
      });
    },
    { threshold: 0.18 }
  );

  document.querySelectorAll(".reveal").forEach(node => observer.observe(node));
}
```

关键点：

- 默认内容可见；
- JavaScript 可用且用户允许动画时，才进入动画初始态；
- 元素出现后停止观察；
- 不因脚本失败导致内容消失。

### 12.11 分场景动效编排

#### 连续长页

推荐节奏：

```text
章节标题进入
→ 结论先出现
→ 图表绘制
→ 解读和来源出现
```

适合：

- `IntersectionObserver`；
- 粘性导航；
- 章节进度；
- 一次性入场。

避免：

- 每次上下滚动都反复播放；
- 大面积视差；
- 滚动被脚本强制接管。

#### 16:9 分页演示

推荐节奏：

```text
页面背景
→ 标题
→ 主证据
→ 解释
→ 行动结论
```

每页只设计一个主动画线索。页面退出动画应短于进入动画。

#### A4 报告

屏幕预览可以有轻动效，但打印状态必须：

- 所有内容立即可见；
- 清除 transform；
- 清除 transition；
- 图表停在最终状态；
- 隐藏编辑和导航 UI。

### 12.12 风格 × 动效组合预设

| 预设名 | 风格元素 | 动效组合 | 适用报告 |
|---|---|---|---|
| `editorial-calm` | 大留白、细线、衬线标题、柔和色块 | Fade + Wipe + Slow Rise | 趋势、审美、品牌白皮书 |
| `swiss-signal` | 网格、粗体、大数字、高纯度强调色 | Fast Rise + Draw + Count | 咨询、战略、数据总结 |
| `data-pulse` | 深色、光点、坐标、玻璃面板 | Scan + Draw + Pulse + Count | AI、科技、发布会 |
| `organic-breathe` | 有机曲线、柔光、低饱和绿色 | Fade + Float + Soft Expand | 健康、食品、可持续 |
| `archive-scan` | 档案编号、纸张、扫描框、印章 | Scan + Reveal + Stamp | 文化、博物馆、城市 |
| `social-snap` | 截图框、标签、贴纸、对比卡 | Quick Stagger + Count + Focus | 社媒周报、内容复盘 |

每个项目先选预设，再根据品牌做 20%–30% 的调整。不要每页换一套动效语言。

### 12.13 元素组合限制

为避免视觉过载，建议：

- 一页最多 1 种背景纹理；
- 一页最多 2 种容器风格；
- 一页最多 1 个持续循环动效；
- 一页最多 2 种进入动作；
- 强调色覆盖面积通常不超过页面的 10%–15%；
- 装饰元素不能与真实数据图形相似；
- 深色玻璃、霓虹、扫描线不要同时高强度出现；
- 正文页优先可读，章节页和封面才承担风格展示。

### 12.14 风格资产登记卡

每沉淀一个元素或动画，都填写：

```md
# 资产登记卡

- 资产 ID：STYLE-CARD-001
- 名称：档案展签卡
- 类型：容器 / 背景 / 强调 / 媒体 / 导航 / 动效
- 风格族：文化档案
- 版本：1.0.0
- 适用形态：长页 / 16:9 / A4
- 适用内容：案例、文物说明、城市节点
- 视觉变量：背景、边框、圆角、字体、间距
- 动效变量：动作、时长、缓动、触发方式
- 深色模式：支持 / 不支持
- 打印模式：支持 / 需降级
- reduced motion：静态替代方式
- 性能成本：低 / 中 / 高
- 依赖：无 / SVG / 图表库 / 图片
- 禁用场景：
- 示例页面：
- 维护人：
- 更新记录：
```

### 12.15 文件组织与命名

```text
design-assets/
├─ tokens/
│  ├─ color.css
│  ├─ type.css
│  ├─ spacing.css
│  └─ motion.css
├─ styles/
│  ├─ editorial/
│  ├─ swiss/
│  ├─ data-tech/
│  ├─ organic/
│  ├─ archive/
│  └─ social/
├─ elements/
│  ├─ backgrounds/
│  ├─ cards/
│  ├─ accents/
│  ├─ media-frames/
│  └─ navigation/
├─ motions/
│  ├─ entrances/
│  ├─ emphasis/
│  ├─ transitions/
│  └─ ambient/
├─ presets/
└─ previews/
```

命名建议：

```text
[风格]-[对象]-[变体]-[状态]

archive-card-label-default
swiss-metric-block-accent
organic-bg-glow-soft
social-tag-positive
data-motion-line-draw
```

### 12.16 版本与复用策略

- `1.0.0`：稳定可用；
- `1.1.0`：新增变体，不破坏旧页面；
- `2.0.0`：结构或变量不兼容；
- 每个资产配一个最小 HTML 示例；
- 每个预设至少配三张预览：
  - 封面；
  - 图表页；
  - 密集内容页；
- 业务数据与资产代码分开存放；
- 素材图片只保留示例，不成为组件依赖；
- 定期淘汰重复资产，避免“组件坟场”。

### 12.17 风格与动效检查清单

- [ ] 已选择一个主风格和最多一个辅助风格。
- [ ] 风格与报告主题、读者和品牌一致。
- [ ] 背景、容器、强调、媒体框架来自同一视觉语言。
- [ ] 装饰元素没有伪装成真实数据。
- [ ] 同类组件跨页保持同一风格。
- [ ] 动效有明确的信息作用。
- [ ] 同一页的动效方向和缓动一致。
- [ ] 关闭动画后内容仍完整可见。
- [ ] reduced motion 模式可用。
- [ ] 打印模式清除了动画状态。
- [ ] 持续动效不会影响阅读。
- [ ] 动效没有导致页面卡顿或图表延迟。
- [ ] 元素已登记 ID、版本、适用场景和禁用场景。
- [ ] 代表页已有截图预览。

### 12.18 风格与动效生成提示词

```text
请为这份 HTML 报告建立一套可复用的“风格元素 × 动效系统”。

主风格：【编辑杂志 / 瑞士信息设计 / 数据科技 / 自然健康 / 文化档案 / 社交运营】
辅助风格：【可选，只能选择一个】
报告形态：【连续长页 / 16:9 / A4】
品牌关键词：【3–5 个】
目标情绪：【专业、克制、活力、未来、温暖等】

请先输出：
1. 风格 DNA；
2. 颜色、字体、形状、材质与动效令牌；
3. 背景、容器、强调、媒体框架和导航元素清单；
4. 页面进入、图表、数字、切页和环境动效清单；
5. 封面、图表页、密集内容页的组合预设；
6. 每项资产的适用场景、禁用场景和打印降级方式。

实现要求：
1. 所有视觉和动效参数使用 CSS 变量；
2. 默认状态下内容可见；
3. 动效只在脚本可用且用户未选择 reduced motion 时启用；
4. 同一页最多两种进入动作和一个持续动效；
5. 打印时移除 transition、animation 和 transform；
6. 生成资产登记卡和最小可运行示例；
7. 不把业务文案、具体数据和图片写死在风格组件中。
```

### 12.19 无障碍降级

必须支持：

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    transition-delay: 0ms !important;
  }

  .reveal,
  [data-motion] {
    opacity: 1 !important;
    transform: none !important;
  }
}
```

打印降级：

```css
@media print {
  *,
  *::before,
  *::after {
    animation: none !important;
    transition: none !important;
  }

  .reveal,
  [data-motion] {
    opacity: 1 !important;
    transform: none !important;
  }

  .ambient-motion,
  .screen-decoration {
    display: none !important;
  }
}
```

---

## 13. 打印与导出：独立的产品状态

### 13.1 打印前检查

- 页面尺寸是否准确；
- 每页是否溢出；
- 背景色是否打印；
- 图表是否已渲染；
- 图片是否清晰；
- 页眉页脚是否重复；
- 页面是否意外多出空白；
- 编辑器、按钮、导航是否隐藏；
- 超链接是否需要显示；
- 版权和来源是否完整。

### 13.2 屏幕样式与打印样式分离

```css
@media screen {
  .print-page {
    margin: 24px auto;
    box-shadow: 0 14px 50px rgb(0 0 0 / 12%);
  }
}

@media print {
  .editor,
  .screen-only,
  .navigation {
    display: none !important;
  }

  .print-page {
    margin: 0;
    box-shadow: none;
  }
}
```

### 13.3 避免无止境追加打印补丁

世界杯样例中大量页面级 `@media print` 修补说明，局部补丁可以救急，但不应成为常规架构。

建议：

- 建立统一的打印组件；
- 每页只允许少量明确变体；
- 使用页面数据属性控制差异；
- 定期清理被覆盖的旧规则；
- 对每一页生成截图并做自动尺寸检查。

---

## 14. 离线与单文件交付

### 14.1 单文件的优点

- 易发送；
- 易归档；
- 无资源丢失；
- 双击可打开；
- 适合客户交付。

### 14.2 单文件的代价

- 体积大；
- 首次解析慢；
- 图片无法浏览器缓存；
- 修改一张图片也要重建全部 HTML；
- 源码审查困难。

### 14.3 推荐工作方式

开发阶段：

```text
src/
├─ index.html
├─ styles/
├─ scripts/
├─ data/
└─ assets/
```

交付阶段：

```text
dist/
├─ report.html
└─ report.pdf
```

通过构建脚本把 CSS、JS、数据和必要图片内嵌，不要直接维护数 MB 的 Base64 源文件。

### 14.4 路径检查

博物馆样例的本地副本暴露了典型问题：HTML 位于资源目录内部，但资源路径又重复指向同名资源目录，导致 CSS 和脚本 404。

交付前必须：

- 从最终交付位置双击打开；
- 断网测试；
- 检查控制台 404；
- 检查中文编码；
- 检查所有相对路径；
- 检查字体和图片降级。

---

## 15. 可访问性与语义

最低要求：

- 页面有唯一 `h1`；
- 标题层级连续；
- 图片有有效 `alt`；
- 图表有文字结论；
- 按钮有明确名称；
- 键盘可操作；
- 正文对比度足够；
- 不只依靠红绿表达正负；
- 导航明确当前章节；
- 表格使用正确的 `th` 和表头作用域。

报告视觉再精美，如果无法搜索、复制、键盘浏览或理解图表，也不是成熟的 HTML 报告。

---

## 16. 建议的工程结构

```text
report-project/
├─ README.md
├─ src/
│  ├─ index.html
│  ├─ styles/
│  │  ├─ tokens.css
│  │  ├─ base.css
│  │  ├─ layout.css
│  │  ├─ components.css
│  │  └─ print.css
│  ├─ scripts/
│  │  ├─ app.js
│  │  ├─ charts.js
│  │  ├─ navigation.js
│  │  └─ editor.js
│  ├─ data/
│  │  └─ report-data.json
│  └─ assets/
│     ├─ images/
│     ├─ icons/
│     └─ fonts/
├─ tools/
│  ├─ build-standalone.*
│  ├─ check-overflow.*
│  └─ render-pages.*
├─ qa/
│  └─ screenshots/
└─ dist/
   ├─ report.html
   └─ report.pdf
```

### 16.1 文件职责

- `tokens.css`：颜色、字体、间距、圆角等视觉变量；
- `base.css`：重置、正文、可访问性；
- `layout.css`：画布、网格、章节；
- `components.css`：卡片、图表、表格；
- `print.css`：A4 和导出；
- `report-data.json`：数据和文案；
- `charts.js`：图表配置与主题；
- `editor.js`：可选，不与阅读模式混杂。

---

## 17. 推荐制作流程

### 阶段 1：定义

- 明确读者；
- 明确阅读场景；
- 明确最终交付格式；
- 选择长页、16:9 或 A4；
- 确定是否离线、是否可编辑。

### 阶段 2：内容

- 整理数据口径；
- 写执行摘要；
- 建立章节；
- 完成“一页一句话”；
- 标记证据和素材缺口。

### 阶段 3：线框

- 先用灰阶排版；
- 确定页面类型；
- 确定组件组合；
- 检查信息密度；
- 不急于加图片和装饰。

### 阶段 4：视觉系统

- 建立设计令牌；
- 确定字体层级；
- 确定图表主题；
- 确定图片风格；
- 制作 3 个代表页作为视觉基准：
  - 封面；
  - 图表页；
  - 密集表格或案例页。

### 阶段 5：实现

- 建立共享数据模型；
- 使用组件渲染；
- 加导航和交互；
- 加打印和离线能力；
- 最后才增加编辑能力。

### 阶段 6：质量验证

- 静态检查；
- 浏览器视觉检查；
- 多尺寸检查；
- 打印/PDF 检查；
- 断网与路径检查；
- 内容数字交叉检查；
- 最终归档。

---

## 18. 质量检查清单

### 18.1 内容

- [ ] 封面说明对象、主题、时间。
- [ ] 执行摘要不超过 5 条。
- [ ] 每页有一个清晰命题。
- [ ] 每个结论都有证据。
- [ ] 每个关键图表都有解释。
- [ ] 数据时间、单位和来源完整。
- [ ] 正文、图表和表格数字一致。
- [ ] 结论能够转化为行动。

### 18.2 视觉

- [ ] 字体层级不超过 6 级。
- [ ] 主色和强调色使用克制。
- [ ] 同一对象跨页保持同色。
- [ ] 页面左边线和网格一致。
- [ ] 卡片圆角、阴影、边框统一。
- [ ] 图片色调和裁切统一。
- [ ] 密集页之后有呼吸节奏。
- [ ] 不依赖装饰掩盖内容问题。

### 18.3 图表与表格

- [ ] 图形选择符合分析任务。
- [ ] 图表标题是结论，不是名词。
- [ ] 重要数值已直接标注。
- [ ] 图例和标签不遮挡。
- [ ] 表格字段已精简。
- [ ] 数字、单位和小数位统一。
- [ ] 极端值、零值和空值显示合理。
- [ ] 打印时图表与表格不被截断。

### 18.4 技术

- [ ] 无控制台错误。
- [ ] 无资源 404。
- [ ] 中文编码正确。
- [ ] 断网仍能按约定打开。
- [ ] 页面无意外横向溢出。
- [ ] 图表随容器变化正确缩放。
- [ ] 动效支持 reduced motion。
- [ ] 键盘导航可用。
- [ ] 编辑 UI 不进入打印。
- [ ] 单文件体积在可接受范围内。

### 18.5 打印与交付

- [ ] 每一页尺寸正确。
- [ ] 每一页没有内容溢出。
- [ ] 页码连续。
- [ ] 页眉、页脚和来源一致。
- [ ] 背景、图片和图表成功输出。
- [ ] PDF 中没有空白页。
- [ ] 最终 HTML 从交付目录打开正常。
- [ ] 原始素材、数据和最终文件一起归档。

---

## 19. 常见反模式

### 19.1 先做视觉，再补内容

结果：页面漂亮，但结论弱、结构反复重做。

修正：先完成“一页一句话”和证据清单。

### 19.2 一个页面塞多个命题

结果：标题无法概括，读者不知道看哪里。

修正：拆页，或建立主结论与辅助证据的明确层级。

### 19.3 为每页写一套独立 CSS

结果：样式覆盖链越来越长，打印补丁无法收敛。

修正：基础组件 + 页面变体 + 少量数据属性。

### 19.4 所有素材都 Base64

结果：文件巨大、修改困难、重复资源浪费。

修正：开发阶段分文件，交付阶段有选择地内嵌并压缩。

### 19.5 混用多个图表库

结果：主题、响应式、打印行为不一致。

修正：项目级统一图表库，特殊图形才使用独立 SVG。

### 19.6 所有元素都可编辑

结果：误操作多、对象状态复杂、导出不可控。

修正：只开放高价值内容块，页面家具锁定。

### 19.7 只检查首屏

结果：后半段出现溢出、空白页、图表空白或图片缺失。

修正：自动渲染每一页，并检查代表性密集页。

---

## 20. 可直接复用的项目启动模板

以后接到 HTML 报告任务时，先填写：

```md
# HTML 报告启动单

## 1. 基本信息
- 报告名称：
- 目标读者：
- 使用场景：
- 汇报时长 / 阅读时长：
- 数据周期：
- 最终交付：HTML / PDF / 两者 / PPTX

## 2. 形态选择
- [ ] 连续长页
- [ ] 16:9 分页演示
- [ ] A4 打印报告
- 是否需要移动端：
- 是否需要离线：
- 是否需要编辑：

## 3. 核心内容
- 最重要的 3–5 条结论：
- 必须出现的数据：
- 必须出现的案例：
- 必须出现的品牌元素：
- 需要生成或搜集的素材：

## 4. 视觉方向
- 关键词：
- 主色：
- 强调色：
- 字体风格：
- 图片风格：
- 参考样例：

## 5. 技术与交付
- 图表库：
- 数据来源：
- 是否单文件：
- PDF 页面规格：
- 浏览器范围：
- 文件体积目标：
```

---

## 21. 可直接复用的生成提示词

```text
请制作一份【报告类型】HTML 报告。

目标读者：【读者】
使用场景：【在线阅读 / 会议演示 / A4 PDF】
核心目标：【希望读者理解或采取什么行动】
交付形态：【连续长页 / 固定 16:9 / A4 210×297mm】

内容要求：
1. 先根据材料提炼 3–5 条执行摘要；
2. 每页或每章节只表达一个主要结论；
3. 所有图表标题写成结论句；
4. 每个结论标注证据、数据口径和来源；
5. 结尾给出明确行动建议。

设计要求：
1. 建立颜色、字号、间距、圆角、阴影的 CSS 变量；
2. 使用统一网格和 6 级以内字体层级；
3. 重点内容高对比，辅助信息降权；
4. 图片统一色调、比例和裁切；
5. 图表跨页保持同一对象同色；
6. 保持足够留白，避免所有页面同等密集。

技术要求：
1. 内容数据与 HTML 结构解耦；
2. 同一项目只使用一个主要图表库；
3. 没有控制台错误、资源 404 和意外横向溢出；
4. 支持断网打开或明确说明在线依赖；
5. 根据交付形态完成响应式、分页或打印验证；
6. 对每一页做溢出检查，并核验图表、图片和中文字体。

素材要求：
请先输出素材清单，包括页面、素材角色、内容描述、比例、安全留白、风格和版权状态；
再生成或配置素材，不要在图片中生成关键文字和 Logo。
```

---

## 22. 建议沉淀的长期资产

### 22.1 三套稳定底座

1. `report-longform-base.html`：连续长页、粘性导航、图表和表格。
2. `report-slide-base.html`：1440 × 810、分页导航、全屏演示。
3. `report-a4-base.html`：210 × 297 mm、页码、打印检查、PDF。

### 22.2 一套共享设计令牌

- 品牌主题；
- 数据语义色；
- 字体层级；
- 空间尺度；
- 卡片与表格规范；
- 图表主题。

### 22.3 一套组件库

- 封面；
- 章节幕；
- 执行摘要；
- 指标卡；
- 结论卡；
- 图表卡；
- 对比卡；
- 案例卡；
- 排名表；
- 机会矩阵；
- 行动路线图；
- 来源与脚注。

### 22.4 一套风格元素与动效资产库

- 编辑杂志、瑞士信息、数据科技、自然健康、文化档案、社交运营六个风格族；
- 背景、空间、容器、强调、媒体、导航六层元素；
- Fade、Rise、Wipe、Draw、Count、Stagger、Focus、Expand 等基础动作；
- 长页、16:9、A4 三类动效编排；
- 风格组合预设、资产登记卡、版本和预览图；
- reduced motion、打印和低性能设备的静态降级。

### 22.5 一套自动检查工具

- 资源路径检查；
- 中文编码检查；
- DOM 重复 ID 检查；
- 横向溢出检查；
- 固定画布溢出检查；
- A4 每页溢出检查；
- 图表渲染检查；
- 页面截图；
- PDF 页数与空白页检查。

### 22.6 一套素材台账

- 素材 ID；
- 使用页面；
- 原始文件；
- 裁切版本；
- 生成提示词；
- 来源；
- 授权；
- 修改记录。

---

## 23. 最终方法论

可以把以后所有 HTML 报告项目压缩成一句工作公式：

```text
明确场景
→ 选择画布
→ 提炼结论
→ 建立证据链
→ 设计页面节奏
→ 配置视觉令牌
→ 用组件和数据模型实现
→ 生成并管理素材
→ 验证屏幕、打印、离线和编辑状态
→ 归档为可复用资产
```

真正可复用的不是某一套颜色或某一个漂亮页面，而是：

- 选择正确形态的能力；
- 把材料转成叙事的能力；
- 用稳定组件表达不同内容的能力；
- 用数据模型降低重复劳动的能力；
- 用自动检查守住交付质量的能力。

---

## 24. 从“生成一份报告”升级为完整生产链

5 个标杆样例解决了“报告应该怎么设计”；后续 5 条项目任务线进一步说明：一份可持续复用的 HTML 报告，必须同时设计生成、编辑和交付。

```text
内容与数据
→ 生成协议
→ HTML 成稿
→ 人工后期编辑
→ 兼容与质量检查
→ HTML / PDF / PPTX 交付
→ 资产回收
```

任何只覆盖中间一段的方案都会留下断点：

- 只重视生成：后期改字、换图、更新数据困难；
- 只重视编辑：原始 HTML 缺少语义，工具只能猜；
- 只重视 PDF：屏幕端和再次编辑能力被忽略；
- 只重视单文件：重复 Base64 导致体积失控；
- 只重视 PPTX 可编辑：复杂视觉还原失败。

因此，报告模板、生成 Skill 与编辑器应共享一套“报告协议”。

## 25. 报告协议：让 HTML 主动表达语义

不要让编辑器只靠视觉和 class 名猜测。建议在不影响浏览器显示的前提下加入稳定标记。

```html
<section class="slide" data-report-page data-pptx-slide>
  <h1 data-report-role="page-title">本页结论</h1>

  <article
    data-report-component="insight-card"
    data-pptx-name="核心洞察卡片 01"
  >
    ...
  </article>

  <div
    data-report-chart
    data-chart-source="data/chart-01.json"
    data-chart-editability="full"
  ></div>

  <img
    data-report-image-slot="case-01"
    data-image-role="evidence"
    alt="案例证据图"
  />
</section>
```

最低建议包含：

- 页面边界：`data-report-page`；
- 内容角色：标题、正文、结论、来源、注释；
- 组件类型与稳定名称；
- 图片槽 ID 与素材角色；
- 图表数据来源和可编辑等级；
- PPTX 图层名、分组和降级模式；
- 水印、Logo、页眉页脚等文档级装饰语义。

语义标记的价值：

- 生成端可做模板约束；
- 编辑器可准确选中和批处理；
- PPTX 可稳定命名和分层；
- 自动检查可判断必填内容；
- 后续换数据时不用重新理解整页 DOM。

## 26. 图片素材策略：独立图片、共享资产、批量槽位

### 26.1 三类图片采用不同策略

| 图片类型 | 推荐策略 | 原因 |
|---|---|---|
| 少量关键内容图 | Base64 单文件内嵌 | 便携、离线、避免路径丢失 |
| 重复 Logo/水印 | 共享资产 + 文档级配置 | 避免每页重复编码 |
| 大量运营截图/案例图 | 图片槽 + 批量确认嵌入 | 降低人工逐张替换成本 |
| 视频和超大动图 | 外部资源包 | 避免 HTML 体积失控 |

Base64 通常增加约三分之一体积。报告启动时应设置体积预算，并检查相同内容是否被重复写入。

### 26.2 图片槽应该成为模板协议

图片槽至少记录：

- 稳定槽位 ID；
- 推荐比例；
- 最小分辨率；
- 图片角色；
- 裁切方式；
- 安全留白；
- 是否允许替换；
- 是否需要来源和版权说明。

批量填图的可靠流程：

```text
选择槽位
→ 选择文件
→ 查看“图片 N → 槽位 N”
→ 调整顺序和裁切
→ 确认写入
→ 自动检查空槽、重复图和低清图
```

不要依赖系统文件选择器的点击顺序。

## 27. 图表交付等级与数据保真

报告制作阶段就应确定图表属于哪一级：

| 等级 | 数据状态 | 编辑能力 | 推荐用途 |
|---|---|---|---|
| A. 结构化图表 | JSON/CSV + 标准引擎配置 | 完整编辑数据、颜色、标题 | 核心数据页 |
| B. 可恢复 SVG | 图形中保留数值、图例或 `<title>` | 经确认后转换 | 自定义图形 |
| C. 静态 SVG | 只有路径、坐标和独立文字 | 整体缩放、移动、导出 | 不再更新的图形 |
| D. 图片 | 只有像素 | 仅替换与裁切 | 截图、复杂视觉 |

### 27.1 核心图表必须保存结构化数据

“肉眼看到数字”不代表之后能还原数据关系。每张核心图表至少归档：

- 分类；
- 系列；
- 精确数值；
- 单位；
- 口径；
- 时间范围；
- 颜色映射；
- 数据来源；
- 缺失值和估算值说明。

避免把“基础盘”“低位”“约 3%–6%”等模糊标签作为唯一数据来源。

### 27.2 自定义 SVG 也要留机器可读线索

若必须手写 SVG，建议加入：

```html
<circle
  cx="120"
  cy="80"
  data-series="宠物"
  data-category="09:00"
  data-value="0.528"
>
  <title>宠物 09:00 = 0.528</title>
</circle>
```

同时将原始数据写在独立 JSON 中。这样既保留自定义视觉，也保留未来编辑能力。

## 28. 样式资产化：样式、组件、主题不要混存

### 28.1 样式

只包含可应用到既有元素的视觉声明：

- 字体、颜色；
- 背景、边框；
- 圆角、阴影；
- 间距；
- 图片框效果。

默认排除具体位置、临时 transform、元素 ID 和项目专属 URL。

### 28.2 组件

依赖稳定内部结构的内容模式：

- KPI 卡；
- 结论卡；
- 图文双栏；
- 案例证据卡；
- 排名表；
- 来源脚注。

组件需要定义必填字段、可选字段、重复器和响应式规则。

### 28.3 主题

控制整份报告的系统级令牌：

- 品牌色和语义色；
- 字体栈与字号级别；
- 圆角、阴影、间距；
- 图表色板；
- 浅色/深色模式；
- 打印状态。

推荐闭环：

```text
从标杆 HTML 提取
→ 清洗高风险属性
→ 相似样式去重
→ 归类为样式 / 组件 / 主题
→ 中性命名
→ 预览与版本登记
→ 在新报告复用
```

资产名称不得携带客户或测试项目名，除非它确实是经授权的客户专属主题。

## 29. PPTX 不是普通导出格式，而是第二排版引擎

PowerPoint 与浏览器的字体度量、表格行高、换行和图层机制不同。HTML 页面在浏览器中不越界，不代表转成可编辑 PPTX 后仍不越界。

多层 CSS 背景也是独立兼容边界。逗号分隔的多个 `linear-gradient()`、透明颜色节点以及根伪元素，不应强行拆成 PowerPoint 原生渐变。更稳妥的混合策略是：只把幻灯片画布背景固化为不透明位图，前景文字、卡片、表格和图表继续保留为可编辑对象。背景捕获必须隔离其他页面、固定控制层和滚动条，并把透明像素先与实际页面底色合成，避免 PowerPoint 暴露透明黑通道。

### 29.1 两种交付模式

- 智能可编辑：标题、正文、表格、图片和形状成为独立对象；
- 高清还原：复杂页面、滤镜、Canvas、WebGL 以高清图层或整页图像交付。

项目开始时就要询问：

> 客户更需要“拿到 PPT 后继续改”，还是“视觉与 HTML 完全一致”？

必要时同时交付两版。

### 29.2 为 PPTX 预留安全设计

- 四周保留约 4% 安全边距；
- 表格不要刚好顶到页面底边；
- 密集表格准备紧凑字号和行高版本；
- 两位数排名列留足宽度；
- 重要标题避免依赖浏览器特有字重；
- 页面中的 Canvas、WebGL 和复杂滤镜准备图片化降级；
- 用 `data-pptx-name` 提供可读图层名；
- 用 `data-pptx-mode="native|image"` 明确局部转换策略。

### 29.3 导出后必须二次检查

最低检查：

- 页数与章节是否一致；
- 标题是否裁切；
- 表格是否由 PowerPoint 自动撑高；
- 文本是否意外换行；
- 图片和图表是否缺失；
- 图层顺序与命名是否正确；
- 16:9、4:3 或竖版尺寸是否正确；
- 在真实 PowerPoint 中打开是否提示修复。

## 30. 报告级水印与品牌装饰

Logo、水印、页眉和页脚属于文档级资产，不应在每页复制一份 Base64。

推荐数据：

```json
{
  "assetId": "brand-logo-main",
  "anchor": "top-right",
  "offsetXmm": 8,
  "offsetYmm": 8,
  "widthMm": 25,
  "opacity": 0.72,
  "pages": "all",
  "showInCanvas": true,
  "showInPrint": true
}
```

设计原则：

- 图片资产只保存一次；
- 页面实例由统一配置生成；
- 封面若需不同 Logo，使用明确的页面范围或覆盖规则；
- 页面 CSS 不得改变水印宽高；
- 水印不能被普通图片批量替换；
- 编辑时可选，平时不阻挡正文；
- 屏幕与打印可分别控制。

## 31. 报告质量检查应覆盖四个状态

过去的检查清单偏向最终页面。完整 QA 应同时覆盖：

| 状态 | 主要检查 |
|---|---|
| 源 HTML | 语义、数据、资源、依赖、编码 |
| 编辑状态 | 选中、改字、换图、图表编辑、撤销 |
| 导出状态 | HTML、PDF、PPTX、打印样式 |
| 成品应用 | 打包后离线打开、字体、vendor、路径 |

### 31.1 建议的真实样本矩阵

- 固定 16:9 多页演示；
- A4 多页报告；
- 超长滚动月报；
- 多表格密集报告；
- ECharts 与 Chart.js 动态报告；
- 自定义 SVG 图表报告；
- 大量图片槽报告；
- 多页重复 Logo/水印报告。

### 31.2 不要只测“成功生成文件”

验证链应包含：

```text
自动测试
→ 浏览器逐页截图
→ 代表性密集页人工检查
→ PDF 页数与空白页
→ PPTX 边界与真实打开
→ 导出结果重新导入
→ 断网或打包版运行
```

## 32. 更新后的项目启动单

在原启动单基础上，新增以下问题：

```md
## 6. 编辑协议
- 哪些文字必须可编辑：
- 哪些组件允许移动：
- 图片槽及顺序：
- 图表可编辑等级：
- 是否需要批量换数据：

## 7. 资产策略
- Base64 体积预算：
- 共享 Logo/水印：
- 外部视频或大文件：
- 字体与第三方库授权：

## 8. 多格式交付
- HTML：在线 / 离线 / 单文件 / 资源包
- PDF：长页 / 智能分页 / A4
- PPTX：可编辑 / 高清还原 / 两版
- PPTX 页面比例：
- 是否要求图层命名：

## 9. 回归样本
- 最长页：
- 最密集表格页：
- 图表最多页：
- 图片最多页：
- 封面/特殊水印页：
```

## 33. 更新后的工作公式

```text
明确读者与行动
→ 选择交付形态
→ 建立叙事和证据链
→ 定义报告语义协议
→ 配置主题、组件和样式
→ 保存结构化数据与素材来源
→ 生成 HTML
→ 在后期工作台人工收尾
→ 检查源文件、编辑态和依赖
→ 分别验证 HTML、PDF、PPTX
→ 回收样式、组件、主题、数据和测试样本
```

真正成熟的复用，不是“下次再复制这份 HTML”，而是让下一份报告从一套可生成、可编辑、可验证、可多格式交付的协议和资产系统开始。

## 34. 图表导出到 PPTX：视觉与数据分层

浏览器图表和 PowerPoint 图表不是同一种绘图系统。ECharts、Chart.js 可以使用渐变、Canvas、脚本 formatter、自定义插件和任意 SVG；PowerPoint 原生 Chart 更强调标准图表类型与可编辑数据。因此图表导出必须先确定交付目标：

| 输出层级 | 视觉一致性 | 数据可编辑 | 适用对象 |
|---|---:|---:|---|
| PowerPoint 原生 Chart | 中高 | 是 | 规则柱线饼、面积、雷达与组合图 |
| DrawingML/SVG 图形 | 高 | 否 | 需要保留复杂外观并允许拆分图形的图表 |
| SVG/PNG 快照 | 最高 | 否 | 渐变、仪表盘、地图、桑基图、WebGL 和自定义图表 |

默认使用 `auto` 策略：只有数据、类型和最终样式都能稳定映射时才生成原生 Chart；出现渐变、圆角柱、逐点复杂配色、脚本 formatter、内联插件、不规则数据或 PowerPoint 不支持的系列时，自动保留视觉快照。

推荐的实现协议为：

```text
图表运行时实例
→ 统一 ChartSpec
→ 类型与视觉兼容性分析
→ Native Chart / DrawingML / Snapshot
→ PPTX 渲染复检
```

设计阶段应遵守：

- 同一系列跨页保持固定纯色；
- 标题、单位、来源和结论尽量放在图表容器外，作为普通文本导出；
- 原生图表不要依赖 Office 主题自动配色；
- 关键坐标轴范围、数字格式和图例位置应显式声明；
- 需要百分百保留品牌设计时，明确使用视觉模式；
- 需要在 PowerPoint 中更新数据时，尽量使用规则的柱、线、饼和面积图；
- 对最终 PPTX 同时检查图表是否可编辑、数据是否正确、视觉是否明显偏离。

对 16:9 演示 HTML，图表容器应有固定宽高并完整位于单页安全区内。系统只负责映射已完成的幻灯片排版，不应在导出阶段重新设计长网页或自动决定图表与文字的版面关系。

## 35. 长文档逐页截图不能依赖滚动位置

当 PPTX 转换需要把复杂渐变、伪元素或浏览器特效烘焙为背景图时，不应使用“滚动到目标页后截取当前视口”的方案。浏览器在文档尾部会限制最大滚动距离，后几页可能无法移动到视口原点，从而重复捕获此前可见页面，最终形成旧底图与可编辑前景叠加。

稳定的逐页捕获协议是：

```text
记录目标页内联布局
→ 隐藏其他页面
→ 将目标页临时固定到视口 (0, 0)
→ 强制隐藏渲染窗口提交当前页面的新合成帧
→ 以目标页真实宽高从固定坐标捕获
→ 写入该页背景
→ 恢复原始布局与可见性
```

交付前至少检查：第一页、文档中段临界页、最后三页和包含 CSS 计数器的页面。多页 PPTX 的验证不能只看文件是否生成或页数是否正确，必须使用 PowerPoint/WPS 实际渲染，确认每页底图唯一、前景没有重复文字、滚动条和编辑控制层未进入背景。
